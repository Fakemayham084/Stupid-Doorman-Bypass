# Stupid-Doorman-Bypass
The plan is when they create the accounts for doorman they let u put a password for your account and the needed infomation to get a firebase auth token is EMAIL (which you already have) and PASSWORD (which you don't). once you have that auth token you send it in the headers of a request to the backend server and all of the endpoints. If i am right which its questionable if you send the token to /api/v1/students/email/ and provide a email it might return your student id and school id which is needed for other parts of the api so i think. theres also another api endpoint /api/v1/tag-classrooms that is used to tell it that you tapped into a class and it sends a id along im thinking if i send a GET request to it i get all of the classes i have back and there for send that i tagged into that class around the right time. i can't do any of this until they create the accounts so i can get the PASSWORD needed to get the AUTH token. i also need to capture a on-demand request and response to i know how to send one back when asked for it.
